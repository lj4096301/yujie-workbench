import { default as React } from 'react';
interface WeatherBackgroundProps {
    code: number;
    isDay: number;
    className?: string;
}
export declare const WeatherBackground: React.FC<WeatherBackgroundProps>;
export {};
